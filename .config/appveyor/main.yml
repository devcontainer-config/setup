version: 1.0.{build}
branches:
  only:
    - main
skip_tags: true
image: Ubuntu2004
build_script:
  - sh: >-
      set -e

      npm install --global @devcontainers/cli

      devcontainer build --workspace-folder .

      devcontainer build --workspace-folder . pnpm build
test_script:
  - sh: >-
      set -e

      devcontainer up --workspace-folder .

      devcontainer exec --workspace-folder . pnpm lint
